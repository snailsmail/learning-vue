### 定制一个cli工具下载

```js
 # 创建一个文件夹
 mkdir vue-auto-router-cli
 cd vue-auto-router-cli
 # 初始化package.json文件
 npm init -y
 # 安装相关命令 
 # commander(命令行界面定制) 
 # download-git-repo(git下载) 
 # ora(命令行进度条) 
 # handlebars(模板引擎，用于代码生成)
 npm install commander download-git-repo oar handlebars -s
```